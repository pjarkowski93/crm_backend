setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: ./Dockerfile
release:
  image: web
  command:
      - python manage.py wait_for_db && python manage.py migrate && python manage.py collectstatic --noinput

run:
  web: python manage.py runserver 0.0.0.0:80
